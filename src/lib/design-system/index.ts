/**
 * MEREM Design System — Public API
 * Import from here in all components.
 *
 * import { C, F, SP, R, SH, TR, Z, L } from '@/lib/design-system'
 */
export { C, F, SP, R, SH, TR, Z, L } from './tokens'
